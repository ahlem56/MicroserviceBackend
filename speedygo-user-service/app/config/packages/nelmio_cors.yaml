# config/packages/nelmio_cors.yaml
nelmio_cors:
  defaults:
    allow_origin: []  # 🚫 Disable public origins (handled by gateway)
    allow_credentials: false
    allow_headers: []
    expose_headers: []
    allow_methods: []
    max_age: 0
  paths:
    '^/': ~
