# ⚙️ Docker Compose for SpeedyGo Microservices
# version: '3.9'  # Optional for newer Docker versions

services:
  # -------------------------------------------------
  # 🚙 Carpooling Microservice
  # -------------------------------------------------
  carpooling-service:
    build: ./Carpooling
    container_name: carpooling-service
    depends_on:
      mysql-carpooling:
        condition: service_healthy   # ✅ Wait until DB is ready
      keycloak:
        condition: service_started
      eureka-server:
        condition: service_started
      config-server:
        condition: service_started
      user-service:
        condition: service_started
    ports:
      - "8086:8086"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    networks:
      - speedygo-network

  # -------------------------------------------------
  # 🗄️ MySQL Database for Carpooling
  # -------------------------------------------------
  mysql-carpooling:
    image: mysql:8.0
    container_name: mysql-carpooling
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ""
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: carpooling_db
    ports:
      - "3307:3306"
    volumes:
      - mysqlcarpooldata:/var/lib/mysql
    networks:
      - speedygo-network
    healthcheck:                     # ✅ Wait for MySQL to be ready
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
  # -------------------------------------------------
  # 🧭 phpMyAdmin (for MySQL Carpooling Database)
  # -------------------------------------------------
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: phpmyadmin
    depends_on:
      - mysql-carpooling
    environment:
      PMA_HOST: mysql-carpooling
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: ""
    ports:
      - "8088:80"   # ✅ Use port 8088 on your host
    networks:
      - speedygo-network
  # -------------------------------------------------
  # 🗄 PostgreSQL (for Symfony User Service)
  # -------------------------------------------------
  db:
    image: postgres:15
    container_name: speedygo-db
    environment:
      POSTGRES_USER: symfony
      POSTGRES_PASSWORD: symfony
      POSTGRES_DB: speedygo_user
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - speedygo-network

  # -------------------------------------------------
  # 🧩 Eureka Server
  # -------------------------------------------------
  eureka-server:
    build: ./eureka-server
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - speedygo-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8761" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s


  # -------------------------------------------------
  # ⚙️ Spring Cloud Config Server
  # -------------------------------------------------
  config-server:
    build: ./config-server
    container_name: config-server
    ports:
      - "8888:8888"
    volumes:
      - ./config-server/config-repo:/config-repo
    depends_on:
      eureka-server:
        condition: service_healthy
    networks:
      - speedygo-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8888/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 5

    # -------------------------------------------------
  # 🔑 Keycloak Authentication Server
  # -------------------------------------------------
  keycloak:
    image: quay.io/keycloak/keycloak:25.0.2
    container_name: keycloak
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KC_DB=dev-file
    command:
      - start-dev
      - --http-port=8080
      - --import-realm
    volumes:
      - ./keycloak:/opt/keycloak/data/import
    ports:
      - "8080:8080"
    networks:
      - speedygo-network

  # -------------------------------------------------
  # 👤 Symfony User Service
  # -------------------------------------------------
  user-service:
    build:
      context: ./speedygo-user-service
    container_name: speedygo-user-service
    depends_on:
      - keycloak
      - eureka-server
      - db
    ports:
      - "8084:8084"
    volumes:
      - ./speedygo-user-service/app:/var/www/html/app   # ✅ Correct folder mapping
    networks:
      - speedygo-network

  # -------------------------------------------------
  # 🚗 Trip Microservice
  # -------------------------------------------------
  trip-service:
    build: ./Trip
    container_name: trip-service
    depends_on:
      - keycloak
      - eureka-server
      - config-server
      - user-service
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    volumes:
      - tripdata:/data       # ✅ Persistent H2 database
    networks:
      - speedygo-network

  # -------------------------------------------------
  # 🌐 API Gateway
  # -------------------------------------------------
  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    depends_on:
      - eureka-server
      - keycloak
      - config-server
      - user-service
      - trip-service
    ports:
      - "8090:8090"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - speedygo-network

# -------------------------------------------------
# 📦 Volumes and Networks
# -------------------------------------------------
volumes:
  pgdata:
  tripdata:             # ✅ Trip persistence
  mysqlcarpooldata:     # ✅ MySQL persistence for Carpooling

networks:
  speedygo-network:
    driver: bridge
